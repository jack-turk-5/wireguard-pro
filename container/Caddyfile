{
	debug
	auto_https off
}

:51819 {
	bind fd/{$CADDY_TCP_FD}
	encode gzip

	handle /api* {
		reverse_proxy unix//run/gunicorn.sock {
			header_up Host {host}
			header_up X-Real-IP {remote_host}
		}
	}

	handle {
		root * /usr/share/caddy/html
		try_files {path} /index.html
		file_server
	}
}
