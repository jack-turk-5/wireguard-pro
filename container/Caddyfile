{
    debug
    # Disable all HTTPS automation (certs + redirects)
    auto_https off
}
http://vpn.jackturk.dev:51819 {
    # Use the inherited systemd socket on FD 3, serve only HTTP/1.1
    bind fd/3 {
        protocols h1
    }
    # Remove any previously set HSTS header so browsers won’t force HTTPS
    header {
        -Strict-Transport-Security
    }
    encode gzip
    # Proxy HTTP requests to your backend on localhost:8000
    reverse_proxy host.containers.internal:51819
}